# ─────────────────────────────────────────────────────────────
# SBC Engine — requirements.txt
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Swiss Ephemeris Python binding (the ONLY external dependency)
# Package name on PyPI is "pyswisseph", imports as "swisseph"
pyswisseph>=2.10.3.1

# ─────────────────────────────────────────────────────────────
# Everything else used by sbc_engine.py is standard library:
#   hashlib, math, dataclasses, datetime, typing  — no install needed
# ─────────────────────────────────────────────────────────────

# Streamlit web app framework
streamlit

# Market data & data processing
yfinance
pandas
